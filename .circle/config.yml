version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: yarn-install
          command: yarn
      - run:
          name: test
          command: yarn test
